
[project]
name = "MIDIGPT-Reaper"
version = "0.1.0"
description = "MIDI-GPT REAPER plugin"
readme = {file = "README.md", content-type = "text/markdown"}
license = {file = "LICENSE"}
requires-python = ">=3.9.0"
authors = [
    { name = "Paul Triana" }
]

dependencies = [
    "symusic",
]